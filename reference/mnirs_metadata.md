# Metadata names of class `"mnirs"`, retrieved with `attr()`

Metadata names of class `"mnirs"`, retrieved with
[`attr()`](https://rdrr.io/r/base/attr.html)

## Usage

``` r
mnirs_metadata
```

## Format

An object of class `character` of length 8.
