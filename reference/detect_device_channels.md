# Detect known channels for a device

Detect known channels for a device

## Usage

``` r
detect_device_channels(
  nirs_device = NULL,
  nirs_channels = NULL,
  time_channel = NULL,
  keep_all = FALSE,
  verbose = TRUE
)
```
