# Rename duplicate strings in a vector with `make.unique()`

Rename duplicate strings in a vector with
[`make.unique()`](https://rdrr.io/r/base/make.unique.html)

## Usage

``` r
rename_duplicates(x)
```
