# Standardise invalid to NA values and round numeric to avoid float precision error

Standardise invalid to NA values and round numeric to avoid float
precision error

## Usage

``` r
clean_invalid(x)
```
