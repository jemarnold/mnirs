# apply span to raw indices and build interval_spec data frame

apply span to raw indices and build interval_spec data frame

## Usage

``` r
apply_span_to_indices(interval_idx, time_vec, span, verbose = TRUE)
```
