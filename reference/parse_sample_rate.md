# Validate and Estimate Sample Rate

Validate and Estimate Sample Rate

## Usage

``` r
parse_sample_rate(
  data,
  file_header,
  time_channel,
  sample_rate = NULL,
  nirs_device = NULL,
  verbose = TRUE
)
```
