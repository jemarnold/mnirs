# resolve a single mnirs_interval object to time values

resolve a single mnirs_interval object to time values

## Usage

``` r
find_interval_time(
  interval,
  time_vec,
  event_vec = NULL,
  position = c("first", "last")
)
```
