# Convert event indices and spans to interval specifications

Convert event indices and spans to interval specifications

## Usage

``` r
specify_intervals(time_vec, event_indices, span, verbose = TRUE)
```
