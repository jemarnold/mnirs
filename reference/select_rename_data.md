# Select data table columns and rename from channels, handling duplicates

Select data table columns and rename from channels, handling duplicates

## Usage

``` r
select_rename_data(
  data,
  nirs_channels,
  time_channel,
  event_channel = NULL,
  keep_all = FALSE,
  verbose = TRUE
)
```
