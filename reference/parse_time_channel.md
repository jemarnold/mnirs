# Parse time_channel character or dttm to numeric

Parse time_channel character or dttm to numeric

## Usage

``` r
parse_time_channel(
  data,
  time_channel,
  add_timestamp = FALSE,
  zero_time = FALSE
)
```
