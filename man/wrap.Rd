% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signif_trailing.R
\name{wrap}
\alias{wrap}
\title{Wrap vector elements}
\usage{
wrap(x, n = 0L)
}
\arguments{
\item{x}{A vector.}

\item{n}{An integer specifying number of elements to move from start to end.
\emph{Default} is \code{0} (no wrapping). Negative values move elements from end
to start.}
}
\value{
A vector with all the same elements as \code{x}.
}
\description{
Rotates vector elements by moving the first \code{n} elements to the end.
}
\details{
The function:
\itemize{
\item Returns \code{x} unchanged if \code{n = 0}.
\item Moves first \code{n} elements to the end of the vector.
\item For negative \code{n}, effectively moves elements from end to start.
\item If \code{n} is larger than \code{length(x)}, positions wrap around.
}
}
\examples{
x <- 1:5
mnirs:::wrap(x, 2)
mnirs:::wrap(x, 0)

## wrapping with n larger than vector length
mnirs:::wrap(x, 7)  ## same as wrap(x, 2)

## negative wrapping (move from end to start)
mnirs:::wrap(x, -1)

## works with any vector type
mnirs:::wrap(letters[1:4], 1)

}
\keyword{internal}
