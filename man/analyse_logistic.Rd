% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_sigmoidal.R
\name{analyse_logistic}
\alias{analyse_logistic}
\title{Analyse logistic kinetics across NIRS channels}
\usage{
analyse_logistic(
  data,
  nirs_channels = NULL,
  time_channel = NULL,
  shape = c("symmetric", "gompertz", "gompertz_left"),
  fix = NULL,
  control = NULL,
  start_time = NULL,
  direction = c("auto", "positive", "negative"),
  end_window = Inf,
  verbose = TRUE,
  ...,
  env = rlang::caller_env()
)
}
\arguments{
\item{data}{A data frame of class \emph{"mnirs"} containing time series data and
metadata.}

\item{nirs_channels}{A character vector giving the names of mNIRS columns to
operate on. Must match column names in \code{data} exactly.
\itemize{
\item If \code{NULL} (default), the \code{nirs_channels} metadata attribute of \code{data} is
used.
}}

\item{time_channel}{A character string naming the time or sample column.
Must match a column name in \code{data} exactly.
\itemize{
\item If \code{NULL} (default), the \code{time_channel} metadata attribute of \code{data} is
used.
}}

\item{shape}{Character; the 4-parameter sigmoidal shape to fit. One of
\code{"symmetric"} (\emph{default}; calls \code{\link[=SSlogistic]{SSlogistic()}}), \code{"gompertz"}
(early-inflection; calls \code{\link[=SSgompertz]{SSgompertz()}}), or \code{"gompertz_left"}
(late-inflection; calls \code{\link[=SSgompertz_left]{SSgompertz_left()}}).}

\item{fix}{An \emph{optional} named list of model parameters (\code{A}, \code{B},
\code{xmid}, \code{slope}) to hold constant during fitting, e.g.
\code{fix = list(A = 0)}. Fixed parameters are excluded from estimation
and reported at their fixed values. Applied to every channel, or
per-channel as a list of lists keyed by channel name, e.g.
\code{fix = list(smo2 = list(A = 0))}.}

\item{control}{An \emph{optional} \code{list()} or \code{\link[stats:nls.control]{stats::nls.control()}} merged
over each fit's internal defaults by \code{\link[=fit_control]{fit_control()}}. Global to all
channels.}

\item{start_time}{A numeric value in units of \code{time_channel} specifying the
response onset (effectively time = \code{0} of the fit). If \code{NULL} (\emph{default}),
retrieves \code{interval_times} from \emph{"mnirs"} metadata, or falls back to \code{0}
or the first positive time value (see \emph{Details}).}

\item{direction}{A character string specifying the response direction
\code{"positive"}, or \code{"negative"}, or detect with \code{"auto"} (\emph{default}). See
\emph{Details}.}

\item{end_window}{A numeric value in units of \code{time_channel} specifying the
window in which to look for the end of the kinetics fit; with no greater/
lesser values within \code{end_window} after the first extrema (min/max).
\code{end_window = Inf} (\emph{default}) returns the global extreme from the full
data range (see \emph{Details}).

For \emph{"biexponential"}, \code{end_window} bounds the fast-phase window only, and
the default is \code{30} sec; the full model is then fit to the full data range.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{...}{Additional arguments passed to the underlying method function.
See \emph{Details}. For the \code{\link[stats:nls]{stats::nls()}} methods (\strong{monoexponential,
exponential_drift, biexponential, sigmoidal, sigmoidal_drift}),
\code{control = list()} can be passed to \code{\link[stats:nls.control]{stats::nls.control()}}, e.g.
\code{control = list(maxiter = 200)}, applied globally to all channels and
intervals.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A \code{data.frame} with one row per \code{nirs_channel} and columns
\code{nirs_channels}, \code{A}, \code{B}, \code{xmid}, \code{slope}, \code{xmid_fitted}.
Per-channel metadata are attached as attributes:
\itemize{
\item \code{"model"}: an \link[stats:nls]{nls} model object, or \code{NULL} for channels
where fitting failed.
\item \code{"fitted_data"}: a named list of per-channel data frames with
columns \code{window_idx} and \code{fitted}.
\item \code{"diagnostics"}: a \code{data.frame} with one row per \code{nirs_channel}
containing model fit diagnostics.
\item \code{"channel_args"}: a \code{data.frame} with one row per \code{nirs_channel}
recording the resolved arguments used.
}
}
\description{
Internal channel-level dispatch for
\code{analyse_kinetics(method = "sigmoidal")}. Fits a 4-parameter sigmoidal
curve to each \code{nirs_channel} within a single \emph{"mnirs"} data frame via
\code{\link[=fit_sigmoidal]{fit_sigmoidal()}} with one of three shapes: \code{"symmetric"}, \code{"gompertz"},
or \code{"gompertz_left"}. See \code{\link[=analyse_kinetics]{analyse_kinetics()}} for user-facing
documentation.
}
\seealso{
\code{\link[=analyse_kinetics]{analyse_kinetics()}}, \code{\link[=logistic]{logistic()}}, \code{\link[=SSlogistic]{SSlogistic()}},
\code{\link[=gompertz]{gompertz()}}, \code{\link[=gompertz_left]{gompertz_left()}}, \code{\link[=SSgompertz]{SSgompertz()}}, \code{\link[=SSgompertz_left]{SSgompertz_left()}}
}
\keyword{internal}
