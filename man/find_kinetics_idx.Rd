% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_kinetics_helpers.R
\name{find_kinetics_idx}
\alias{find_kinetics_idx}
\title{Find valid model-fitting indices up to the first extreme}
\usage{
find_kinetics_idx(
  x,
  t = seq_along(x),
  end_fit_span = 20,
  direction = c("auto", "positive", "negative"),
  ...
)
}
\arguments{
\item{x}{A numeric vector of the response variable.}

\item{t}{An \emph{optional} numeric vector of the predictor variable (time or
sample number). Default is \code{seq_along(x)}.}

\item{end_fit_span}{A numeric value in units of \code{t} specifying the
forward-looking window used to check for subsequent greater/lesser
values than the candidate extreme.}

\item{direction}{A character string specifying the slope direction to
detect — \code{"auto"} (\emph{default}), \code{"positive"}, or \code{"negative"}. See
\emph{Details}.}

\item{...}{Additional arguments.}
}
\value{
A named list with three elements:
\describe{
\item{\code{direction}}{Character; the resolved direction used —
\code{"positive"} (peak) or \code{"negative"} (trough).}
\item{\code{extreme}}{Integer or \code{NULL}; the index of the
first qualifying peak or trough in original \code{x}
space, or \code{NULL} if no qualifying extreme was found
(monotonic, horizontal, or degenerate input).}
\item{\code{idx}}{Integer vector of all valid finite indices,
truncated at \code{t[extreme] + end_fit_span}.}
}
}
\description{
Filters \code{x} and \code{t} to valid finite values, locates the first valid peak
(maximum) or trough (minimum) where \code{t >= 0}, and returns the integer
indices of all finite observations up to \code{end_fit_span} past that extreme.
}
\details{
\subsection{Direction detection}{

When \code{direction = "auto"}, the net slope across all of \code{x} is computed
to determine the overall trend. If positive, the function searches for
a peak (maximum). If negative, it searches for a trough (minimum). When
the net slope is zero or \code{NA}, the direction is determined by comparing
\code{abs(max(x))} to \code{abs(min(x))}, with ties defaulting to \code{"positive"}.
}

\subsection{Negative time handling}{

Only samples where \code{t >= 0} are used for detecting the extreme, allowing
pre-baseline (negative time) data to be excluded from the search.
However, indices where \code{t < 0} are included in the returned vector
provided they are finite.
}
}
\keyword{internal}
