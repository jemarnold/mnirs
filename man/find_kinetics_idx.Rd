% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{find_kinetics_idx}
\alias{find_kinetics_idx}
\title{Find valid model-fitting indices up to the first extreme}
\usage{
find_kinetics_idx(
  x,
  t = seq_along(x),
  end_window = Inf,
  direction = c("auto", "positive", "negative"),
  ...,
  env = rlang::caller_env()
)
}
\arguments{
\item{x}{A numeric vector of the response variable.}

\item{t}{An \emph{optional} numeric vector of the predictor variable (e.g. time).
Default is \code{seq_along(x)}.}

\item{end_window}{A numeric value in units of \code{time_channel} or \code{t}
specifying the forward-looking window used to check for subsequent
greater/lesser values than the candidate extreme. \code{end_window = Inf}
(\emph{default}) returns the global extreme from the full range of \code{x}.}

\item{direction}{A character string specifying the response direction
\code{"positive"}, or \code{"negative"}, or detect with \code{"auto"} (\emph{default}). See
\emph{Details}.}

\item{...}{Additional arguments.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A named list with three elements:
\describe{
\item{\code{direction}}{Character; the resolved direction used --
\code{"positive"} (peak) or \code{"negative"} (trough).}
\item{\code{extreme}}{Integer or \code{NULL}; the index of the
first qualifying peak or trough in original \code{x}
space, or \code{NULL} if no qualifying extreme was found
(monotonic, horizontal, or degenerate input).}
\item{\code{idx}}{Integer vector of all valid finite indices,
truncated at \code{t[extreme] + end_window}.}
}
}
\description{
Filters \code{x} and \code{t} to valid finite values, locates the first valid peak
(maximum) or trough (minimum) where \code{t >= 0}, and returns the integer
indices of all finite observations up to \code{end_window} past that extreme.
}
\details{
\subsection{Direction detection}{

When \code{direction = "auto"}, the excursions of \code{x} above and below its
initial baseline (the median of the earliest samples) are compared via
\code{\link[=detect_direction]{detect_direction()}}. If the upward excursion dominates, the function
searches for a peak (maximum); if the downward excursion dominates, a
trough (minimum). When the excursions tie, the direction is determined
by comparing \code{abs(max(x))} to \code{abs(min(x))}, with ties defaulting to
\code{"positive"}.
}

\subsection{Negative time handling}{

Only samples where \code{t >= 0} are used for detecting the extreme, allowing
pre-baseline (negative time) data to be excluded from the search.
However, indices where \code{t < 0} are included in the returned vector
provided they are finite.
}
}
\keyword{internal}
