% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{init_fixed}
\alias{init_fixed}
\title{Wrap a self-start initialiser to support fixed parameters}
\usage{
init_fixed(init, params)
}
\arguments{
\item{init}{A \code{selfStart} initial function \verb{(mCall, data, LHS, ...)}.}

\item{params}{Character vector of the model parameter names.}
}
\value{
A function suitable for the \code{initial} argument of
\code{\link[stats:selfStart]{stats::selfStart()}}.
}
\description{
Decorates a \code{selfStart} \code{initial} function so parameters supplied as
values in the model formula (e.g. \code{SSmonoexponential(t, A = 0, B, tau)}) are
excluded from the returned start vector. \code{\link[stats:nls]{stats::nls()}} reads the
free parameters from the names of that vector, so excluded
parameters are treated as constants in the formula. Fixed values are
forwarded to the wrapped initialiser as a \code{fixed} list argument to
seed the remaining free estimates.
}
\keyword{internal}
