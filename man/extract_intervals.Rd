% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_intervals.R
\name{extract_intervals}
\alias{extract_intervals}
\title{Extract intervals from \emph{{mnirs}} data}
\usage{
extract_intervals(
  data,
  nirs_channels = NULL,
  time_channel = NULL,
  event_channel = NULL,
  sample_rate = NULL,
  group_intervals = c("distinct", "ensemble"),
  group_channels = NULL,
  start = NULL,
  end = NULL,
  span = list(c(-60, 60)),
  zero_time = FALSE,
  verbose = TRUE,
  event_groups = deprecated()
)
}
\arguments{
\item{data}{A data frame of class \emph{"mnirs"} containing time series data and
metadata, or a \code{list()} of such data frames.}

\item{nirs_channels}{A character vector of mNIRS channel names to operate
on. Names must match column names in \code{data} exactly.
\itemize{
\item If \code{NULL} (default), channels are retrieved from \emph{"mnirs"} metadata.
\item \ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Passing a \code{list()} for per-group
channel selection is deprecated; use \code{group_channels}.
}}

\item{time_channel}{A character string naming the time or sample column.
Must match a column name in \code{data} exactly.
\itemize{
\item If \code{NULL} (default), the \code{time_channel} metadata attribute of \code{data} is
used.
}}

\item{event_channel}{An \emph{optional} character string giving the name of an
event/lap column. The column may contain character event labels or integer
lap numbers.
\itemize{
\item Required when using \code{\link[=by_label]{by_label()}} or \code{\link[=by_lap]{by_lap()}} for \code{start} or \code{end}.
\item Retrieved from metadata if not defined explicitly.
}}

\item{sample_rate}{An \emph{optional} numeric sample rate (Hz) used to bin time
values for ensemble-averaging. If \code{NULL}, will be estimated from
\code{time_channel} (see \emph{Details}).}

\item{group_intervals}{Either a character string or a non-empty \code{list()} of
non-empty integer-valued numeric vectors specifying how to group intervals
(see \emph{Details}). Custom indices must be between \code{1} and the number of
detected intervals.
\describe{
\item{\code{"distinct"}}{The default. Extract each interval as an independent
data frame.}
\item{\code{"ensemble"}}{Ensemble-average each specified \code{nirs_channel} across
all detected intervals, returning a single data frame.}
\item{\code{list(c(1, 2), c(3, 4))}}{Ensemble-average each specified
\code{nirs_channel} within each group and return one data frame per group.}
}}

\item{group_channels}{A character vector or a \code{list()} of character
vectors selecting which \code{nirs_channels} are ensemble-averaged within
each interval group (see \emph{Details}).
\itemize{
\item If \code{NULL} (default), all \code{nirs_channels} are used for every group.
\item Only relevant when \code{group_intervals} contains \emph{"ensemble"}-averaged
intervals; with \code{group_intervals = "distinct"} no channel processing
occurs.
}}

\item{start}{Specifies where intervals begin. Either raw values -- numeric
for time values, character for event labels, explicit integer (e.g. \code{2L})
for lap numbers -- or created with \code{\link[=by_time]{by_time()}}, \code{\link[=by_label]{by_label()}}, \code{\link[=by_lap]{by_lap()}},
or \code{\link[=by_sample]{by_sample()}}. Multiple specifications can be combined with
\code{list()} (e.g. \code{list(by_time(30), by_label("go"))}); see \emph{Details}.}

\item{end}{Specifies where intervals end. Either raw values -- numeric for
time values, character for event labels, explicit integer (e.g. \code{2L})
for lap numbers -- or created with \code{\link[=by_time]{by_time()}}, \code{\link[=by_label]{by_label()}}, \code{\link[=by_lap]{by_lap()}},
or \code{\link[=by_sample]{by_sample()}}. Multiple specifications can be combined with
\code{list()} (e.g. \code{list(by_time(30), by_label("go"))}); see \emph{Details}.}

\item{span}{A one- or two-element numeric vector expanding the time bounds
around \code{c(start, end)}, in units of \code{time_channel}; or a \code{list()} of such
vectors. (\emph{default} \code{span = c(-60, 60)}. Applied additively to interval
boundaries:
\itemize{
\item When both \code{start} and \code{end} are specified: \code{span[1]} shifts start times,
\code{span[2]} shifts end times.
\item When only \code{start} or only \code{end} is specified: both \code{span[1]} and
\code{span[2]} apply as a window around the event).
\item A single \emph{positive} value is recycled to shift the end times (e.g.
\code{span = 60} -> \code{c(0, 60)}).
\item A single \emph{negative} value is recycled to shift the start times (e.g.
\code{span = -60} -> \code{c(-60, 0)}).
}}

\item{zero_time}{Logical. Default is \code{FALSE}. If \code{TRUE}, re-calculates
numeric \code{time_channel} values to start from zero within each interval
data frame.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{event_groups}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Renamed to
\code{group_intervals} for naming consistency across the package.}
}
\value{
A named \code{list()} of \link[tibble:tibble-package]{tibbles} of class
\emph{"mnirs"}, each with metadata available via \code{attributes()}. When \code{data}
is a list of data frames, results are flattened into a single-layer
list with interval names indicating nested number of data frame and
interval as \verb{interval_<df>.<interval>}. Other interval names (e.g.
\code{"ensemble"} or custom group names) are suffixed as \verb{<name>_<df>}.
}
\description{
Extract intervals from \emph{"mnirs"} time series data, specifying interval
start and end boundaries by time value, event label, lap number, or sample
index.
}
\details{
\subsection{Interval specification}{

Interval \code{start} and \code{end} boundaries are specified using helper functions,
or by passing raw values directly:

\describe{
\item{\code{\link[=by_time]{by_time()}}}{Time values in units of \code{time_channel}.}
\item{\code{\link[=by_label]{by_label()}}}{Strings to match in \code{event_channel}.
All matching occurrences are returned.}
\item{\code{\link[=by_lap]{by_lap()}}}{Lap numbers to match in \code{event_channel}. Resolves to the
first sample of each lap for \code{start}, and the last lap sample for \code{end}}
\item{\code{\link[=by_sample]{by_sample()}}}{Integer sample indices (row numbers).}
}

Raw values supplied to \code{start}/\code{end} are auto-coerced:
\itemize{
\item Numeric -> \code{\link[=by_time]{by_time()}}
\item Character -> \code{\link[=by_label]{by_label()}},
\item Explicit integer (e.g. \code{2L}) -> \code{\link[=by_lap]{by_lap()}}.
\item Use \code{\link[=by_sample]{by_sample()}} explicitly for sample indices.
}

\code{start} and \code{end} can use different specification types (e.g., start by
label, end by time). When lengths differ, the shorter is recycled.

Multiple specification types can be combined for a single boundary with
\code{list()} (e.g. \code{start = list(by_time(30), by_label("go"))}).
Resolved boundary times are concatenated in the order supplied. Combined
specifications must use the \code{by_} helpers directly: raw values are ignored
with a warning.
}

\subsection{Time span window}{

\code{span} additively expands the time span window around interval boundaries.
\itemize{
\item A two-value vector expands the \code{start} and \code{end}, respectively:
\code{span = c(-60, 60)} expands the \code{start} earlier by \code{60}, and the \code{end}
later by \code{60}. For example,
\verb{start = by_time(30), end = by_time(60), span = c(-5, 10)} returns an
interval of \verb{[25, 70]}.
\item A single numeric value is recycled according to the sign: \code{span = -60}
becomes \code{c(-60, 0)} to expand the \code{start} earlier. \code{span = 60} becomes
\code{c(0, 60)} to expand the \code{end} later.
\item If only \code{start} is specified alone, both span values expand the single
boundary window: \verb{start = by_time(30), span = c(-5, 60)} returns
\verb{[25, 90]}.
}
}

\subsection{Per-group channel selection with group_channels}{

When \code{group_intervals = "ensemble"} or a list of numeric grouped intervals,
\code{group_channels} can be specified as a list of column names to override
which channels are ensemble-averaged within each group. For example, to
exclude a channel from one interval:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{group_channels = list(
  c(A, B, C),
  c(A, C) ## channel "B" data are excluded from the second interval
)
}\if{html}{\out{</div>}}

If all grouped intervals include all \code{nirs_channels}, \code{group_channels} can
be left as \code{NULL} (the default) and all channels are ensemble-averaged
within every group.
}

\subsection{Grouping intervals}{

\code{group_intervals} controls whether extracted intervals are returned as
distinct data frames or ensemble-averaged.

\describe{
\item{\code{"distinct"}}{The default. Extract each interval and return a
list of independent data frames.}
\item{\code{"ensemble"}}{Ensemble-average each specified \code{nirs_channel} across
all detected intervals and return a one-item list with a single data
frame.}
\item{\code{list(c(1, 2), c(3, 4))}}{Ensemble-average each specified
\code{nirs_channel} within each group and return a list with one data frame
for each group. Any intervals detected but not specified in
\code{group_intervals} are returned as distinct.}
}

\code{group_intervals} lists can be named (e.g.
\code{list(low = c(1, 2), high = c(3, 4))}) and will pass those names to the
returned list of data frames.

When \code{group_intervals} is a list of numeric interval numbers, list items in
\code{group_channels} and \code{span} are recycled to the number of groups. If lists
are only partially specified, the final item is recycled forward as needed.
Extra items are ignored.
}
}
\examples{
## read example data
data <- read_mnirs(
    example_mnirs("train.red"),
    nirs_channels = c(
        smo2_left = "SmO2 unfiltered",
        smo2_right = "SmO2 unfiltered"
    ),
    time_channel = c(time = "Timestamp (seconds passed)"),
    zero_time = TRUE,
    verbose = FALSE
) |>
    ## avoid issues ensemble-averaging irregular samples
    resample_mnirs(method = "linear", verbose = FALSE)

## ensemble-average across multiple intervals
interval_list <- extract_intervals(
    data,                         ## channels recycled to all intervals by default
    nirs_channels = c(smo2_left, smo2_right),
    group_intervals = "ensemble", ## ensemble-average across two intervals
    start = by_time(368, 1084),   ## manually identified interval start times
    span = c(-20, 90),            ## include the last 180-sec of each interval (recycled)
    zero_time = TRUE              ## re-calculate common time to start from `0`
)

interval_list[[1L]]

\donttest{
  if (requireNamespace("ggplot2", quietly = TRUE)) {
    plot(interval_list, time_labels = TRUE) +
      ggplot2::geom_vline(xintercept = 0, linetype = "dotted")
  }
}

}
