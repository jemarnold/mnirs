% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_monoexponential.R
\name{fit_monoexponential}
\alias{fit_monoexponential}
\title{Fit a monoexponential model to a numeric vector}
\usage{
fit_monoexponential(
  x,
  t = seq_along(x),
  t0 = 0,
  use_time_delay = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A numeric vector of the response variable.}

\item{t}{An \emph{optional} numeric vector of the predictor variable (e.g. time).
Default is \code{seq_along(x)}.}

\item{t0}{A numeric value specifying the start of the kinetics response
in units of \code{t}. Observations where \code{t <= t0} define the baseline window.
Defaults to \code{0}.}

\item{use_time_delay}{Logical; default is \code{TRUE} to attempt to fit a
4-parameter \code{\link[=SS_monoexp4]{SS_monoexp4()}} model (A, B, tau, TD) with a time delay.
If the 4-parameter fit fails, or if \code{use_time_delay = FALSE}, fits a
reduced 3-parameter \code{\link[=SS_monoexp3]{SS_monoexp3()}} model (A, B, tau).}

\item{verbose}{Logical. Default is \code{TRUE}. Will display or silence (if
\code{FALSE}) warnings and information messages helpful for troubleshooting. A
global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{...}{Additional arguments. \code{bypass_checks} suppresses input
validation.}
}
\value{
A named list containing:
\item{\code{model}}{The fitted \code{\link[stats:nls]{stats::nls()}} model object, or \code{NULL} on
convergence failure.}
\item{\code{A}, \code{B}, \code{tau}}{Fitted model coefficients.}
\item{\code{TD}}{Time-delay relative to \code{t0} (4-parameter), or \code{NA_real_}
for a 3-parameter fit.}
\item{\code{MRT}, \code{HRT}}{Mean and half response times relative to \code{t0}.}
\item{\code{tau_fitted}, \code{MRT_fitted}, \code{HRT_fitted}}{Predicted response
values at \code{tau}, \code{MRT}, and \code{HRT}.}
\item{\code{fitted}}{A numeric vector of predicted values the same length
as \code{x}.}
\item{\code{n_params}}{Integer; \code{4L} or \code{3L}, or \code{NA_integer_} on failure.}
}
\description{
Fit a monoexponential curve to a numeric vector \code{x} against a predictor
vector \code{t} via \code{\link[stats:nls]{stats::nls()}} using a self-starting model. Attempts a
4-parameter fit (\code{\link[=SS_monoexp4]{SS_monoexp4()}}) by default, with fallback to a
3-parameter fit (\code{\link[=SS_monoexp3]{SS_monoexp3()}}) on convergence failure.
}
\seealso{
\code{\link[=analyse_monoexponential]{analyse_monoexponential()}}, \code{\link[=monoexponential]{monoexponential()}},
\code{\link[=SS_monoexp3]{SS_monoexp3()}}, \code{\link[=SS_monoexp4]{SS_monoexp4()}}
}
