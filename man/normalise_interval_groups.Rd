% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_interval_helpers.R
\name{normalise_interval_groups}
\alias{normalise_interval_groups}
\title{Normalise custom interval grouping to a complete named list}
\usage{
normalise_interval_groups(
  group_intervals,
  n_intervals,
  verbose = TRUE,
  env = rlang::caller_env()
)
}
\arguments{
\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\description{
Adds intervals missing from a custom \code{group_intervals} list as
single-interval groups, warns on duplicates, and names groups by
user-supplied names with \verb{interval_<ids>} fallback.
}
\keyword{internal}
