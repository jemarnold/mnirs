% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_monoexponential.R
\name{monoexp_start}
\alias{monoexp_start}
\title{Grid-profiled starting estimates for the monoexponential model}
\usage{
monoexp_start(x, t, fixed = list(), has_TD = FALSE)
}
\arguments{
\item{x, t}{Numeric vectors of the response and time.}

\item{fixed}{A named list of user-fixed parameter values, which narrow
the grids and constrain the free estimates.}

\item{has_TD}{Logical; include the time delay \code{TD}.}
}
\value{
A named numeric vector of starting estimates in model order.
}
\description{
Vector-level initialiser behind \code{\link[=monoexp_init]{monoexp_init()}}, called directly by the
kinetics worker on the fit window. Profiles \code{tau} (and \code{TD} for the
4-parameter model) on a coarse grid and keeps the RSS-minimising start
(cf. \code{\link[=biexp_start]{biexp_start()}}). The model is linear in \code{A} and \code{B} once \code{tau}
and \code{TD} are held, so the asymptotes are solved by least squares at
every grid point at once. Point estimates from derivative changepoints
or log-linearisation are too sensitive to noise, overshoot, and plateau
data on real NIRS signals, and can strand nls with a singular gradient.
}
\keyword{internal}
