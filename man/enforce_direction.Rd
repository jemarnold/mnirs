% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{enforce_direction}
\alias{enforce_direction}
\title{Enforce the requested direction on a converged parametric fit}
\usage{
enforce_direction(
  model,
  coefs,
  fit_data,
  direction,
  amp_fn,
  fn = sub("^(SS)?", "SS", as.character(amp_fn)),
  lower = NULL,
  upper = NULL,
  floor_params = NULL,
  fix = list(),
  control = NULL,
  .nirs,
  interval_name,
  env = rlang::caller_env()
)
}
\arguments{
\item{model}{A converged \link[stats:nls]{nls} model object.}

\item{coefs}{Named numeric coefficient vector in model parameter
order with fixed values merged in (see \code{\link[=full_coefs]{full_coefs()}}).}

\item{fit_data}{Data frame with the response in the first column and
time in the second; the refit formula is built on those names.}

\item{direction}{Character; resolved \code{"positive"} or \code{"negative"}.}

\item{amp_fn}{Symbol; model fn taking \code{t} and the model parameters as
named arguments. A self-start fn returning a \code{"gradient"} attribute
(free symbols, asymptotes first) makes both refits analytic; a plain
fn falls through to \code{\link[stats:numericDeriv]{stats::numericDeriv()}}.}

\item{fn}{Character; the self-start fn named in the warning
(\emph{default} \verb{SS<amp_fn>}, or \code{amp_fn} itself when already prefixed).}

\item{lower, upper}{Named numeric bounds for free parameters other
than the asymptotes. Sign-floor bounds should be data-scaled
small values (not \code{.Machine$double.eps}) so pinned-floor
degeneracy is detectable.}

\item{floor_params}{Character; names of refit coefficients subject
to the pinned-floor degeneracy check. \code{NULL} (\emph{default}) checks
every finite bound; restrict when other bounds are structural
(e.g. the biexponential time-constant bounds).}

\item{fix}{Named list of user-fixed parameter values.}

\item{control}{User \code{\link[stats:nls.control]{stats::nls.control()}} list merged over the refit
defaults by \code{\link[=fit_control]{fit_control()}}.}

\item{.nirs}{Character; the channel name.}

\item{interval_name}{Character; the interval label.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A named list \code{list(model, coefs)} with \code{coefs} a named
numeric vector in \verb{(A, B, ...)} space including fixed values, or
\code{NULL} when the direction cannot be satisfied (caller returns
\code{\link[=build_na_results]{build_na_results()}}).
}
\description{
Direction is the sign of the primary amplitude \code{D = B - A} (\code{B - A}
for the biexponential), where the primary asymptote follows \code{A} in
model parameter order. A fit is satisfied when \code{D} and every free
parameter lie inside the refit box (\code{D} sign-constrained; \code{lower}/
\code{upper} for the rest, e.g. a sigmoid slope sign floor) and is
returned unchanged. Otherwise the model is refit on \code{D} via
\code{nls(algorithm = "port")} with \code{D} sign-bounded and its magnitude
floored strictly above zero (sigmoid models divide by \code{D}), then
re-expressed in the original parameterisation from that optimum so
the returned model reports consistent coefficient names. A refit
that fails, pins a sign-floored coefficient (degenerate flat fit),
or loses the requested sign on re-expression warns and returns
\code{NULL}. Parameters in \code{fix} are held constant: a fixed \code{A} or \code{B} is
substituted into the amplitude reparameterisation; with both fixed
the amplitude sign is predetermined and no refit is possible.
}
\keyword{internal}
