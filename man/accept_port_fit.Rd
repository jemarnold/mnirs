% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{accept_port_fit}
\alias{accept_port_fit}
\title{Accept or reject a non-converged port fit}
\usage{
accept_port_fit(model, on_error, ok = TRUE)
}
\arguments{
\item{model}{An \link[stats:nls]{nls} model or \code{NULL}.}

\item{on_error}{A reporting function; see \code{\link[=fit_td_fallback]{fit_td_fallback()}}.}

\item{ok}{Logical; a further acceptance condition, e.g. an RSS no worse
than the starting estimates. Evaluated only for a non-converged fit.}
}
\value{
\code{model} or \code{NULL}.
}
\description{
\code{\link[stats:nls]{stats::nls()}} with \code{algorithm = "port"} and \code{warnOnly = TRUE} returns
a model whose stop certificate failed. It is kept with a warning when
\code{ok} holds and its coefficients are finite; otherwise it is reported as
an error and dropped. The port stop code is reported in prose either
way.
}
\keyword{internal}
