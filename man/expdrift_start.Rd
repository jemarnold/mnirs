% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_exponential_drift.R
\name{expdrift_start}
\alias{expdrift_start}
\title{Grid-profiled starting estimates for the exponential-drift model}
\usage{
expdrift_start(x, t, fixed = list(), has_TD = FALSE)
}
\arguments{
\item{x, t}{Numeric vectors of the response and time.}

\item{fixed}{A named list of user-fixed parameter values, which narrow
the grids and constrain the free estimates.}

\item{has_TD}{Logical; include the time delay \code{TD}.}
}
\value{
A named numeric vector of starting estimates in model order.
}
\description{
Vector-level initialiser behind \code{\link[=expdrift_init]{expdrift_init()}}, called directly by
the kinetics worker on the fit window. Profiles \code{tau} (and \code{TD}) on a
coarse grid and keeps the RSS-minimising start (cf.
\code{\link[=monoexp_start]{monoexp_start()}}). The model is linear in \code{A}, \code{B}, and \code{slope_B} once
\code{tau} and \code{TD} are held, so those are solved by least squares at every
grid point at once via \code{\link[=solve_grid3]{solve_grid3()}}. User-fixed \code{tau}, \code{TD}, and
\code{drift_fraction} narrow the grids; the linear parameters are always solved
free, as this is only a seed. \code{tau} is capped so the drift onset stays
inside the record; a grid point whose hinge has no support is singular
and skipped.
}
\keyword{internal}
