% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_blood_volume.R
\name{correct_blood_volume}
\alias{correct_blood_volume}
\title{Correct for blood volume changes}
\usage{
correct_blood_volume(
  data,
  oxy_channel = NULL,
  deoxy_channel = NULL,
  total_channel = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of class \emph{"mnirs"} containing time series data
and metadata, a list of data frames, or a grouped data frame (see
\emph{Details}).}

\item{oxy_channel}{A character vector naming the \code{oxy[haem]} (oxygenated
haemoglobin and myoglobin; \emph{O2Hb}) column(s) in \code{data}. Must match exactly.}

\item{deoxy_channel}{A character vector naming the \code{deoxy[haem]}
(deoxygenated haemoglobin and myoglobin; \emph{HHb}) column(s) in \code{data}.
Must match exactly.}

\item{total_channel}{A character vector naming the \code{total[haem]} (total
haemoglobin and myoglobin; \emph{THb}; proxy for blood volume) column(s) in
\code{data}. Must match exactly.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}
}
\value{
A \link[tibble:tibble-package]{tibble} of class \emph{"mnirs"} with blood
volume-corrected channels written back to the specified columns, and with
metadata available with \code{attributes()}. For list or grouped data frame
input, returns a named list of \emph{"mnirs"} tibbles, one per interval.
}
\description{
Normalises mNIRS channels for the effects of blood volume changes, following
the sample-wise iterative method of \emph{Beever & Tripp et al, 2020}.
}
\details{
\subsection{Specify NIRS component channels}{

At least two of \code{oxy_channel}, \code{deoxy_channel}, and \code{total_channel} must
be specified to calculate the blood volume correction factor. Best practice
is to specify all existing channels in \code{data}. Missing channels are derived
from the specified pair before the correction is applied.
\itemize{
\item \code{total} = \code{oxy + deoxy}
\item \code{oxy}   = \code{total - deoxy}
\item \code{deoxy} = \code{total - oxy}
}

Multiple channel pairs can be corrected in one call by passing equal-length
vectors, with each element number forming a pair (e.g.
\verb{oxy_channel = c(o2hb_1, o2hb_2), deoxy_channel = c(hhb_1, hhb_2)}).

\emph{NOTE}: the returned data frame will \emph{ONLY} include corrected values for
the specified channels. Non-specified channels will remain uncorrected and
will therefore no longer be comparable to corrected channels. Best practice
is to specify all existing channels in \code{data}.
}

\subsection{Compute blood volume correction}{

If any NIRS channels have negative values, all specified channels will be
ensemble-shifted by a common offset so that all channels contain only
positive values. Relative scaling across channels is preserved. This is
modified from the method in \emph{Beever & Tripp et al, 2020} to properly
calculate \code{total[haem]} and the blood volume correction factor \code{beta} when
there are negative NIRS values.

The correction factor \code{beta} is effectively the single-channel fractional
(\%) oxygen saturation used to normalise \code{oxy[haem]} and \code{deoxy[haem]}
relative to an adjusted invariant \code{total[haem]}. This is computed as the
cumulative sum of adjusted incremental differences:

\deqn{\Delta\text{O2Hb}_c = \Delta\text{O2Hb} - \beta \cdot \Delta\text{THb}}
\deqn{\Delta\text{HHb}_c = \Delta\text{HHb} - 
(1 - \beta) \cdot \Delta\text{THb}}

After correction, \code{total[haem]} is zero (blood volume changes are
normalised).
}
}
\section{Data input formats}{


\emph{{mnirs}} processing functions accept \code{data} in multiple formats:
\itemize{
\item A \strong{single \emph{"mnirs"} data frame} is processed and returned directly.
\item A \strong{list of \emph{"mnirs"} data frames}: each interval is processed
separately and returned as a named list.
\item A \strong{grouped \emph{"mnirs"} data frame}, e.g. with \code{dplyr::group_by()}: the
data frame is split by grouping levels and each group is processed as
a separate interval, returned as a named list.
}
}

\examples{
data <- read_mnirs(
    file_path = example_mnirs("artinis"),
    nirs_channels = c(o2hb = 2, hhb = 3),
    time_channel = c(sample = 1),
    verbose = FALSE,
)

plot(data)

result <- correct_blood_volume(
    data,
    oxy_channel = "o2hb",
    deoxy_channel = "hhb", ## thb will be derived from o2hb + hhb
)

plot(result)

}
\references{
Beever AT, Tripp TR, Zhang J, MacInnis MJ (2020) Nirs-Derived Skeletal
Muscle Oxidative Capacity Is Correlated with Aerobic Fitness and
Independent of Sex. J Appl Physiol (1985).
\doi{10.1152/japplphysiol.00017.2020}

Ryan TE, Erickson ML, Brizendine JT, et al. (2012) Noninvasive Evaluation of
Skeletal Muscle Mitochondrial Capacity with near-Infrared Spectroscopy:
Correcting for Blood Volume Changes. J Appl Physiol (1985).
\doi{10.1152/japplphysiol.00319.2012}
}
