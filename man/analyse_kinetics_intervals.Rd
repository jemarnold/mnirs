% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{analyse_kinetics_intervals}
\alias{analyse_kinetics_intervals}
\title{Run a kinetics worker over each interval and collate results}
\usage{
analyse_kinetics_intervals(
  data,
  method,
  worker_args,
  nirs_quo,
  time_quo,
  group_intervals,
  zero_time,
  verbose,
  call,
  env,
  fallback = TRUE
)
}
\arguments{
\item{data}{A data frame, list of data frames, or grouped data frame.}

\item{method}{Character; the canonical method name.}

\item{worker_args}{Named list of method-specific arguments passed
to the method's worker in \code{kinetics_workers}.}

\item{nirs_quo, time_quo}{Quosures of the caller's \code{nirs_channels}
and \code{time_channel} arguments, captured in the method frame.}

\item{group_intervals}{\code{"ensemble"} or a \code{list()} of sample index
vectors; see \code{\link[=split_kinetics_groups]{split_kinetics_groups()}}.}

\item{zero_time}{Logical; if \code{TRUE}, rebases each interval's
\code{time_channel} to start from zero, shifting \code{interval_times} metadata
by the same offset.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{call}{The matched call from the user-facing method.}

\item{env}{The call recorded for condition reporting.}

\item{fallback}{Logical; resolve the method's fallback chain in
\code{kinetics_fallbacks} per channel (see \code{\link[=analyse_kinetics_channels]{analyse_kinetics_channels()}}).}
}
\value{
An \emph{"mnirs_kinetics"} object from
\code{\link[=build_kinetics_results]{build_kinetics_results()}}.
}
\description{
Shared skeleton for \verb{analyse_kinetics.*} methods: normalises \code{data}
to a named list of interval data frames, splits sample groups via
\code{\link[=split_kinetics_groups]{split_kinetics_groups()}}, calls the method worker in
\code{kinetics_workers} once per interval, and collates results via
\code{\link[=build_kinetics_results]{build_kinetics_results()}}.
}
\keyword{internal}
