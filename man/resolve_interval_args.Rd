% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{resolve_interval_args}
\alias{resolve_interval_args}
\title{Resolve per-interval arguments}
\usage{
resolve_interval_args(
  worker_args,
  interval_names,
  chan_names,
  verbose = TRUE,
  env = rlang::caller_env()
)
}
\arguments{
\item{worker_args}{Named list of method-specific arguments.}

\item{interval_names}{Character vector of interval names from
\code{\link[=as_data_list]{as_data_list()}}.}

\item{chan_names}{Character vector of resolved channel names, used only
to give channel keys precedence over interval keys.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A named list with one element per interval; each element is the
\code{worker_args} list resolved for that interval.
}
\description{
Peels the interval layer from \code{worker_args} before per-interval worker
dispatch, mirroring the per-channel convention of
\code{\link[=resolve_channel_args]{resolve_channel_args()}}. An argument is treated as an interval map when
it is a \code{list()} with at least one named key matching an interval name,
no key matching a channel name (channel maps keep their existing
per-channel meaning), and at most one unnamed element acting as the
fallback for unlisted intervals. \code{fix} must additionally be a list of
lists, so a plain parameter list (e.g. \code{fix = list(A = 0)}) stays global.
}
\details{
Resolved values may themselves be per-channel maps, which pass untouched
to \code{\link[=resolve_channel_args]{resolve_channel_args()}} downstream. Intervals omitted from a map with
no unnamed fallback resolve to \code{NULL}, falling through to the argument's
default, matching omitted-channel behaviour.
}
\keyword{internal}
