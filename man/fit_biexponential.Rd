% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_biexponential.R
\name{fit_biexponential}
\alias{fit_biexponential}
\title{Fit a biexponential model to one channel}
\usage{
fit_biexponential(x, t, valid, .a, ctx)
}
\arguments{
\item{x, t}{Numeric vectors of the channel response and time elapsed
from \code{start_time}.}

\item{valid}{The \code{\link[=find_kinetics_idx]{find_kinetics_idx()}} result for the channel.}

\item{.a}{The resolved argument list of the channel.}

\item{ctx}{The channel context list of \code{\link[=analyse_kinetics_channels]{analyse_kinetics_channels()}}.}
}
\value{
The \code{coefs}/\code{model}/\code{fitted_data}/\code{diag} list of
\code{\link[=build_fit_results]{build_fit_results()}}, or \code{\link[=build_na_results]{build_na_results()}} when a stage fails.
}
\description{
Channel fitter of \code{\link[=analyse_biexponential]{analyse_biexponential()}} (see
\code{\link[=analyse_kinetics_channels]{analyse_kinetics_channels()}}), in two stages. Stage 1 fits the fast
phase as a monoexponential on the \code{end_window} window
(\code{\link[=fit_monoexponential]{fit_monoexponential()}}). Stage 2 fits the full \code{\link[=SSbiexponential]{SSbiexponential()}}
model on the whole response via \code{\link[stats:nls]{stats::nls()}} with
\code{algorithm = "port"}, \code{A}, \code{tau}, and \code{TD} box-bounded about their
stage-1 values by the \verb{*_flex} half-widths and \code{B}, \code{B2}, \code{tau2} free,
seeded by \code{\link[=biexp_start]{biexp_start()}} with the fast phase held. A failed stage
returns \code{NA}, and the fallback chain resolves the row upstream.
}
\keyword{internal}
