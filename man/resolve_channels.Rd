% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mnirs_helpers.R
\name{resolve_channels}
\alias{resolve_channels}
\title{Resolve channels from user input, device defaults, or the Oxysoft legend}
\usage{
resolve_channels(
  raw,
  device,
  user,
  keep_all = FALSE,
  verbose = TRUE,
  env = rlang::caller_env()
)
}
\arguments{
\item{raw}{A raw character data frame from \code{read_file()}.}

\item{device}{Output of \code{detect_mnirs_device()}.}

\item{user}{A list of user-specified \code{time}, \code{event}, and \code{nirs} channels,
each a named character vector \code{c(new = "original")} or \code{NULL}.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A list of \code{time}, \code{extra}, \code{event}, \code{labels}, and \code{nirs} channel
mappings, each a named \code{c(new = "original")} vector or \code{NULL}. List
order sets output column order.
}
\description{
User-specified channels take priority; for Artinis, user originals given
as legend names (cleaned or raw trace) resolve to their column ids.
Otherwise \code{nirs} channels are read
from the Oxysoft legend (Artinis) or header cells starting with \code{SmO2};
\code{time} falls back to the device default, and is detected later by
\code{detect_time_channel()} when still \code{NULL}; \code{event} falls back to the
device default when present in the header row. Device companion columns
(\code{extra}, \code{labels}) are returned only with \code{keep_all = TRUE}.
}
\keyword{internal}
