% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_sigmoidal.R
\name{sigmoid_core}
\alias{sigmoid_core}
\alias{sigmoid_model}
\title{Sigmoid curve with gradient}
\usage{
sigmoid_core(t, A, B, xmid, slope, shape)

sigmoid_model(mCall, t, A, B, xmid, slope, shape)
}
\arguments{
\item{t}{A numeric vector of the predictor variable (time).}

\item{A}{A numeric parameter for the starting asymptote of the response
variable.}

\item{B}{A numeric parameter for the ending asymptote of the response
variable.}

\item{xmid}{A numeric parameter for the time at the \emph{inflection point} (the
steepest point) of the curve, in units of the predictor variable \code{t}.}

\item{slope}{A numeric parameter for the response rate \code{dx/dt} at the
inflection \code{xmid}.}

\item{shape}{Character; the 4-parameter sigmoidal shape. One of
\code{"symmetric"} (\emph{default}; \code{\link[=logistic]{logistic()}}), \code{"gompertz"} (\code{\link[=gompertz]{gompertz()}}), or
\code{"gompertz_left"} (\code{\link[=gompertz_left]{gompertz_left()}}).}

\item{mCall}{A matched call to the model function.}
}
\value{
\code{\link[=sigmoid_core]{sigmoid_core()}}: a list of the curve \code{val}, the partial
derivatives by parameter name, and the rate \code{k}. \code{\link[=sigmoid_model]{sigmoid_model()}}: a
numeric vector of predicted values with a \code{"gradient"} attribute when
any parameter is free.
}
\description{
\code{\link[=sigmoid_core]{sigmoid_core()}} evaluates a 4-parameter sigmoid of the given \code{shape}
and its partial derivatives on the canonical parameters, shared by the
\code{selfStart} model functions of \code{\link[=SSlogistic]{SSlogistic()}}, \code{\link[=SSgompertz]{SSgompertz()}},
\code{\link[=SSgompertz_left]{SSgompertz_left()}}, and \code{\link[=SSsigmoidal_drift]{SSsigmoidal_drift()}}. Every shape is a
function \code{W(u)} of \code{u = k * (t - xmid)} with rate \code{k = c * slope / (B - A)} (\code{c = 4} symmetric, \code{e} Gompertz), so with \code{P = dW/du} the
partials share one form. \code{\link[=sigmoid_model]{sigmoid_model()}} attaches the gradient over
the parameters written as bare symbols in \code{mCall} (see
\code{\link[=free_params]{free_params()}}), so \code{\link[stats:nls]{stats::nls()}} skips \code{\link[stats:numericDeriv]{stats::numericDeriv()}}.
}
\keyword{internal}
