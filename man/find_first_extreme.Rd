% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_kinetics_helpers.R
\name{find_first_extreme}
\alias{find_first_extreme}
\title{Find the first extreme peak or trough value in a numeric vector to constrain
model fit}
\usage{
find_first_extreme(
  x,
  t = seq_along(x),
  end_fit_span = 20,
  direction = c("auto", "positive", "negative"),
  ...
)
}
\arguments{
\item{x}{A numeric vector of the response variable.}

\item{t}{An \emph{optional} numeric vector of the predictor variable (time or
sample number). Default is \code{seq_along(x)}.}

\item{end_fit_span}{A numeric value in units of \code{t} specifying
the forward-looking window used to check for subsequent greater/lesser
values than the candidate extreme.}

\item{direction}{A character string specifying the slope direction to
detect — \code{"auto"} (\emph{default}), \code{"positive"}, or \code{"negative"}. See
\emph{Details}.}

\item{...}{Additional arguments.}
}
\value{
A numeric index at the last index where
\code{t <= t[extreme_idx] + end_fit_span}, constrained by positive
values of \code{t} at the start, and the end range of \code{x}.
}
\description{
Locate the first peak (maximum) or trough (minimum) in \code{x} such that
no value within the following \code{end_fit_span} time span exceeds it.
}
\details{
\subsection{Direction detection}{

When \code{direction = "auto"}, the net slope across all of \code{x} is computed
to determine the overall trend. If positive, the function searches for
a peak (maximum). If negative, it searches for a trough (minimum). When
the net slope is zero or \code{NA}, the direction is determined by comparing
\code{abs(max(x))} to \code{abs(min(x))}, with ties defaulting to \code{"positive"}.

Only samples where \code{t > 0} are processed, allowing pre-baseline
(negative time) data to be excluded from the search.
}
}
\keyword{internal}
