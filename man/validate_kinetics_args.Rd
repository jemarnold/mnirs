% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{validate_kinetics_args}
\alias{validate_kinetics_args}
\title{Validate resolved per-channel kinetics arguments}
\usage{
validate_kinetics_args(
  per_channel,
  data,
  t_vec,
  verbose = TRUE,
  env = rlang::caller_env()
)
}
\arguments{
\item{per_channel}{Named list of resolved argument lists, one per channel.}

\item{data}{A data frame of class \emph{"mnirs"} containing time series data and
metadata.}

\item{t_vec}{Numeric vector of \code{time_channel} values.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
The \code{per_channel} list with mutating validators applied.
}
\description{
Validates each channel's resolved argument list once, before any fitting,
so an invalid argument fails fast rather than after an expensive fit on an
earlier channel. Validation is keyed on which arguments are present.
Mutating validators are applied and written back:
\code{\link[=validate_start_time]{validate_start_time()}} clamps \code{start_time}, and \code{align} is matched to its
choices. Verbose hints are emitted for the first channel only to avoid
repeating identical messages.
}
\keyword{internal}
