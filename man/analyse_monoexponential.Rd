% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monoexponential.R
\name{analyse_monoexponential}
\alias{analyse_monoexponential}
\title{Analyse monoexponential kinetics per channel}
\usage{
analyse_monoexponential(
  data,
  nirs_channels = NULL,
  time_channel = NULL,
  monoexp_params = 3L,
  algorithm = c("default", "plinear", "port"),
  control = list(),
  channel_args = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame of class \emph{"mnirs"} containing time series data and
metadata.}

\item{nirs_channels}{A character vector giving the names of mNIRS columns to
operate on. Must match column names in \code{data} exactly.
\itemize{
\item If \code{NULL} (default), the \code{nirs_channels} metadata attribute of \code{data} is
used.
}}

\item{time_channel}{A character string giving the name of the time or sample
column. Must match a column name in \code{data} exactly.
\itemize{
\item If \code{NULL} (default), the \code{time_channel} metadata attribute of \code{data} is
used.
}}

\item{monoexp_params}{Integer; \code{3L} (\emph{default}) for \code{\link[=SS_monoexp3]{SS_monoexp3()}}
(A, B, tau) or \code{4L} for \code{\link[=SS_monoexp4]{SS_monoexp4()}} (A, B, tau, TD).}

\item{algorithm}{Character; \code{\link[stats:nls]{stats::nls()}} algorithm — \code{"default"}
(\emph{default}), \code{"plinear"}, or \code{"port"}.}

\item{control}{A named list of control parameters passed to
\code{\link[stats:nls.control]{stats::nls.control()}}.}

\item{channel_args}{An \emph{optional} \code{list()} named by \code{nirs_channels} with
unique per-channel arguments to override global default arguments (see
\emph{Details}).}

\item{verbose}{Logical. Default is \code{TRUE}. Will display or silence (if
\code{FALSE}) warnings and information messages helpful for troubleshooting. A
global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{...}{Additional arguments.}
}
\value{
A data frame of model coefficients (one row per channel)
with attributes \code{"predicted"}, \code{"diagnostics"}, \code{"channel_args"}.
}
\description{
Fit a monoexponential curve to each \code{nirs_channel} within a single
data frame. Called by \code{\link[=analyse_kinetics]{analyse_kinetics()}} when
\code{method = "monoexponential"}.
}
\keyword{internal}
