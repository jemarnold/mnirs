% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_biexponential.R
\name{biexp_core}
\alias{biexp_core}
\alias{biexp_model}
\title{Biexponential model with gradient}
\usage{
biexp_core(t, A, B, tau, B2, tau2, TD = NULL)

biexp_model(t, A, B, tau, B2, tau2, TD = NULL)
}
\arguments{
\item{t}{A numeric vector of the predictor variable (time).}

\item{A}{A numeric parameter for the starting value of the response
variable (the \code{t = 0} intercept).}

\item{B}{A numeric parameter for the asymptote of the \emph{fast} component; the
value the fast response alone would approach.}

\item{tau}{A numeric parameter for the \emph{fast} time constant (\eqn{\tau_1}),
in units of the predictor variable \code{t}. Dominates the initial steep
response.}

\item{B2}{A numeric parameter for the asymptote of the \emph{slow} component;
the stable plateau the response recovers toward as \code{t} approaches
infinity.}

\item{tau2}{A numeric parameter for the \emph{slow} time constant (\eqn{\tau_2}),
in units of the predictor variable \code{t}. Typically \verb{tau2 >> tau}.}

\item{TD}{A numeric parameter for the \emph{time delay} before the onset of the
response, in units of the predictor variable \code{t}. If \code{NULL} (\emph{default}),
a 5-parameter model without time delay is used.}
}
\value{
\code{\link[=biexp_core]{biexp_core()}}: a list of the curve \code{val} and the partial
derivatives by parameter name. \code{\link[=biexp_model]{biexp_model()}}: a numeric vector of
predicted values with a \code{"gradient"} attribute when any parameter is
free.
}
\description{
\code{\link[=biexp_core]{biexp_core()}} evaluates the curve and its partial derivatives on the
canonical parameters. \code{\link[=biexp_model]{biexp_model()}} is the model function of
\code{\link[=SSbiexponential]{SSbiexponential()}}: \code{\link[=biexponential]{biexponential()}} plus the gradient for the
parameters written as bare symbols in the call (see \code{\link[=free_params]{free_params()}}),
so \code{\link[stats:nls]{stats::nls()}} skips \code{\link[stats:numericDeriv]{stats::numericDeriv()}}.
}
\keyword{internal}
