% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{split_kinetics_groups}
\alias{split_kinetics_groups}
\title{Split interval data frames into sample groups}
\usage{
split_kinetics_groups(
  data_list,
  group_intervals,
  verbose = TRUE,
  env = rlang::caller_env()
)
}
\arguments{
\item{data_list}{Named list of data frames from \code{\link[=as_data_list]{as_data_list()}}.}

\item{group_intervals}{\code{"ensemble"} or a \code{list()} of integer-valued
sample index vectors; see \code{\link[=analyse_kinetics]{analyse_kinetics()}}.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A named list of data frames, one per group per data frame.
}
\description{
Applies the \code{group_intervals} argument of \code{\link[=analyse_kinetics]{analyse_kinetics()}} to a
named list of data frames. \code{"ensemble"} returns \code{data_list} unchanged;
a \code{list()} of sample (row) indices subsets every data frame into one
interval per group. Samples in no group are dropped and samples in
several groups are warned about. Group names become interval names
(\verb{interval_<n>} when unnamed), suffixed \verb{<group>_<df>} when
\code{data_list} holds more than one data frame. Row-subset intervals no
longer correspond to their \code{interval_times}/\code{interval_span} metadata,
so those attributes are dropped.
}
\keyword{internal}
