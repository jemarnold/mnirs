% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{solve_grid3}
\alias{solve_grid3}
\title{Batched 3-parameter least squares over a grid}
\usage{
solve_grid3(g11, g12, g13, g22, g23, g33, b1, b2, b3, xx)
}
\arguments{
\item{g11, g12, g13, g22, g23, g33}{Gram entries of the three basis columns.}

\item{b1, b2, b3}{Inner products of the basis columns with the response.}

\item{xx}{The response sum of squares \verb{<x, x>}.}
}
\value{
A list with the coefficient arrays \code{c1}, \code{c2}, \code{c3} and the
residual sum of squares \code{rss}, which is \code{Inf} where the system is
singular.
}
\description{
Solves the normal equations of a 3-column linear model at every grid
point at once, given the Gram entries \verb{g_ij = <c_i, c_j>} and
right-hand sides \verb{b_i = <c_i, x>} as equal-shaped arrays (one element
per grid point). Used by the self-start initialisers to profile the
non-linear parameters on a grid without a per-point decomposition.
}
\keyword{internal}
