% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signif_trailing.R
\name{signif_trailing}
\alias{signif_trailing}
\alias{signif_whole}
\alias{signif_pvalue}
\title{Format numbers for display as character strings}
\usage{
signif_trailing(x, digits = 2L, format = c("digits", "signif"))

signif_whole(x, digits = 5L)

signif_pvalue(
  x,
  digits = 3L,
  format = c("digits", "signif"),
  display = c("value", "symbol"),
  symbol = "*",
  symbol.repeat = FALSE,
  alpha = 0.05
)
}
\arguments{
\item{x}{A numeric vector.}

\item{digits}{An integer specifying the number of \emph{decimal places} or
\emph{significant figures} to preserve. Negative \code{digits} values will round to
the nearest whole value of \code{10^(digits)}.}

\item{format}{Indicates how to treat \code{digits}. Either the desired number of
decimal places (\code{format = "digits"}, the \emph{default}) or significant figures
after the decimal place (\code{format = "signif"}).}

\item{display}{Specifies output type, either \emph{"value"} (the \emph{default}) for
formatted numbers or \emph{"symbol"} for significance symbols.}

\item{symbol}{Character string specifying the significance symbol.
\emph{Default} is "*".}

\item{symbol.repeat}{Logical indicating whether to repeat symbols for
different significance levels. Default is \emph{FALSE}.}

\item{alpha}{A numeric value specifying significance threshold.
\emph{Default} is \code{0.05}.}
}
\value{
\code{signif_trailing()} returns a character vector of formatted numbers
the same length as \code{x}.

\code{signif_whole()} returns a numeric vector the same length as \code{x}.

\code{signif_pvalue()} returns a character vector of formatted p-values
or significance symbols the same length as \code{x}.
}
\description{
\code{signif_trailing()} converts numeric values to character strings to
preserve trailing zeroes

\code{signif_whole()} rounds numeric values to a specified number of significant
figures, or the nearest whole value if the number of digits of \code{x} are
greater than \code{digits}.

\code{signif_pvalue()} displays p-values as either formatted numeric strings
or significance symbols.
}
\details{
\code{signif_trailing()}
\itemize{
\item Negative \code{digits} round to the respective integer place, e.g.
\code{signif_trailing(123, digits = -1)} returns \code{"120"}.
}

Decimal rounding is based on the "banker's rounding" default behaviour of
\code{signif()} and \code{round()}, where \code{signif(123.45, 4)} or \code{round(123.45, 1)}
each return \code{123.4}.

\code{signif_whole()}
\itemize{
\item Negative \code{digits} round to the nearest whole value as if \code{digits = 0},
e.g. \code{signif_whole(123, digits = -5)} still returns \code{123}.
}

\code{signif_pvalue()}
\itemize{
\item When \code{display = "value"} and e.g. \code{digits = 3}, \code{x} will be either
rounded to 3 decimal places with \code{signif_trailing()}, or appear as e.g.
\emph{"< 0.001"}.
\item \code{digits = 1} will display \emph{"less than \code{alpha}"}, e.g. \emph{"< 0.05"}.
\item When \code{display = "symbol"}, if \code{symbol.repeat = TRUE}: Uses repeated
symbols based on thresholds
\verb{(0.001 = "***", 0.01 = "**", alpha = "*", ns = "")}.
\item If \code{symbol.repeat = FALSE}: Shows one symbol \code{"*"} for p < alpha,
otherwise empty string.
}
}
\examples{
x <- c(123, 123.4, 123.45)

mnirs:::signif_trailing(x, digits = 1, format = "digits")
mnirs:::signif_trailing(x, digits = -1, format = "digits")
mnirs:::signif_trailing(x, digits = 5, format = "signif")
mnirs:::signif_trailing(x, digits = 0, format = "signif")

x <- c(123, 123.4, 123.45)

mnirs:::signif_whole(x, digits = 5)
mnirs:::signif_whole(x, digits = 2)
mnirs:::signif_whole(x, digits = -1)

p_vals <- c(0.0001, 0.003, 0.02, 0.08, 0.15)

## format as numeric
mnirs:::signif_pvalue(p_vals)
mnirs:::signif_pvalue(p_vals, digits = 1)

## format as symbols
mnirs:::signif_pvalue(p_vals, display = "symbol")
mnirs:::signif_pvalue(p_vals, display = "symbol", symbol.repeat = TRUE)

## custom alpha and symbol
mnirs:::signif_pvalue(p_vals, display = "symbol", alpha = 0.01, symbol = "†", symbol.repeat = TRUE)

}
\seealso{
\code{\link[=formatC]{formatC()}} \code{\link[=round]{round()}} \code{\link[=signif]{signif()}}
}
\keyword{internal}
