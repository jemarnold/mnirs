% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mnirs_helpers.R
\name{select_channels}
\alias{select_channels}
\title{Select, rename, and order channel columns}
\usage{
select_channels(
  data,
  channels,
  keep_all = FALSE,
  verbose = TRUE,
  env = rlang::caller_env()
)
}
\arguments{
\item{data}{The named character data table.}

\item{channels}{A list of named \code{c(new = "original")} channel mappings by
role from \code{resolve_channels()}; \code{NULL} roles are dropped.}

\item{verbose}{Logical. \code{TRUE} (\emph{default}) will display, and \code{FALSE} will
silence warnings and information messages helpful for troubleshooting.
Global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A list of the selected \code{data} and \code{channels} as new names by
role.
}
\description{
Original names are made unique to match \code{rename_duplicates(names(data))};
duplicated new names are made unique with a warning. Channel names take
priority over clashing names of other data columns. Columns are ordered
by role, followed by all remaining columns when \code{keep_all = TRUE}.
}
\keyword{internal}
