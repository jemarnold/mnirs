% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.mnirs.R
\name{plot.mnirs_kinetics}
\alias{plot.mnirs_kinetics}
\title{Plot \emph{{mnirs}} kinetics results}
\usage{
\method{plot}{mnirs_kinetics}(x, fitted = TRUE, markers = TRUE, labels = TRUE, ...)
}
\arguments{
\item{x}{An \emph{"mnirs_kinetics"} object from \code{\link[=analyse_kinetics]{analyse_kinetics()}}.}

\item{fitted}{Logical. Default is \code{TRUE}; overlays a dashed fitted curve for
parametric methods (\code{"peak_slope"}, \code{"monoexponential"},
\code{"exponential_drift"}, \code{"biexponential"}, \code{"sigmoidal"},
\code{"sigmoidal_drift"}) in a darker shade of the channel
colour. \code{"response_time"} has no fitted curve.}

\item{markers}{Logical. Default is \code{TRUE}; draws a dotted vertical line at
the response onset (\code{start_time}) and key coefficient points in a darker
shade of the channel colour.}

\item{labels}{Logical. Default is \code{TRUE}; annotates each panel with the key
coefficient value(s) for the fitted method, in the right-hand corner
the observed signal leaves clear.}

\item{...}{Additional arguments.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2} object.
}
\description{
Create a default plot for an \emph{"mnirs_kinetics"} object returned from
\code{\link[=analyse_kinetics]{analyse_kinetics()}}. Observed signals are drawn per \code{nirs_channel}, faceted
by interval, with the fitted response overlaid and the key kinetics
coefficient(s) annotated per panel.
}
\details{
Accepts some arguments in \code{...}, such as \code{label_size} passed to
\code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}. Also accepts args passed to \code{\link[=plot.mnirs]{plot.mnirs()}}, such as
\code{points}, \code{time_labels}, \code{nrow}, \code{ncol}, or \code{scales}.

A method with no annotation spec in \code{\link[=kinetics_annotations]{kinetics_annotations()}} plots the
observed signal and fitted curve only, without markers or labels.
}
\examples{
\dontshow{if (rlang::is_installed("ggplot2")) withAutoprint(\{ # examplesIf}
result <- read_mnirs(
    example_mnirs("train.red"),
    nirs_channels = c(smo2 = "SmO2"),
    time_channel = c(time = "Timestamp (seconds passed)"),
    zero_time = TRUE,
    verbose = FALSE
) |>
    resample_mnirs(method = "linear", verbose = FALSE) |>
    extract_intervals(
        group_intervals = "distinct",
        start = by_time(368, 1084),
        span = c(-20, 90),
        zero_time = TRUE,
        verbose = FALSE
    ) |>
    analyse_kinetics(
        method = "peak_slope",
        span = 10,
        verbose = FALSE
    )

plot(result)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=analyse_kinetics]{analyse_kinetics()}}, \code{\link[=plot.mnirs]{plot.mnirs()}}
}
